return {
  "Tsuzat/NeoSolarized.nvim",
  lazy = true,
  opts = {
    style = o.background,
    transparent = true,
    terminal_colors = true,
    enable_italics = true,
    styles = {
      functions = { bold = true },
      underline = true,
      undercurl = false,
    },
  },
}
