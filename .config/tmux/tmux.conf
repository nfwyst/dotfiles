set -sg escape-time 5
set -g default-terminal "tmux-256color"
set -g status-interval 60
set -as terminal-features ',xterm*:RGB'
set -as terminal-features ',xterm-ghostty:clipboard:cstyle:focus:overline:RGB:strikethrough:sync:usstyle'

unbind r
bind r source-file $HOME/.config/tmux/tmux.conf

unbind C-b
set -g prefix M-p
bind M-p send-prefix
set -g mouse on
bind \` switch-client -t'{marked}'
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
setw -g mode-keys vi

set -g status-position bottom
set -g default-shell /opt/homebrew/bin/nu
set -g focus-events on

# kill pane: 只 SIGKILL nu 的 children (starship/zoxide/atuin/carapace),
# 不碰 nu 本身 — nu 收到 TERM 会向 pgid 转发, 兄弟 pane 共享 pgid 会被一锅端。
# children 不清掉则 tmux 在 POLLHUP 自旋 100% CPU。
bind x run-shell -b ' \
  PID=#{pane_pid}; \
  for CHILD in $(pgrep -P "$PID" 2>/dev/null); do \
    for GRAND in $(pgrep -P "$CHILD" 2>/dev/null); do \
      kill -KILL "$GRAND" 2>/dev/null; \
    done; \
    kill -KILL "$CHILD" 2>/dev/null; \
  done; \
  true' \; kill-pane

# panel select
bind -n M-h select-pane -L
bind -n M-l select-pane -R
bind -n M-j select-pane -D
bind -n M-k select-pane -U

# open window in current directory
bind c new-window -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# panel resizing
bind -r h resize-pane -L
bind -r l resize-pane -R
bind -r j resize-pane -D
bind -r k resize-pane -U

# go to last session
bind M-p switch-client -l

# move window to left
bind -n M-i swap-window -t -1

# move window to right
bind -n M-o swap-window -t +1

# use visual selection mode as vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# set env
setenv -g PATH "/opt/homebrew/bin:$PATH"
setenv -g SNACKS_KITTY "1"
setenv -g TERMINAL "ghostty"
setenv -g XDG_CONFIG_HOME "$HOME/.config"
setenv -g XDG_BIN_HOME "$HOME/.local/bin"
setenv -g XDG_DATA_HOME "$HOME/.local/share"
setenv -g GIT_CONFIG_GLOBAL "$HOME/.config/.gitconfig"

# enable passthrough
set -g allow-passthrough on

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'wfxr/tmux-power'
set -g @plugin 'alexwforsythe/tmux-which-key'
set -g @plugin 'erikw/tmux-dark-notify'

# dark/light theme auto-switch (requires: brew install cormacrelf/tap/dark-notify)
set -g @dark-notify-theme-path-light '$HOME/.config/tmux/tmux-light.conf'
set -g @dark-notify-theme-path-dark '$HOME/.config/tmux/tmux-dark.conf'

# default theme (overridden by dark-notify on macOS)
set -g @tmux_power_theme               'everforest'
set -g @tmux_power_right_arrow_icon    ' '
set -g @tmux_power_left_arrow_icon     ' '
set -g @tmux_power_left_a              ''
set -g @tmux_power_right_y             '%H:%M:%S'
set -g @tmux_power_right_z             '%m-%d•%A'

run '$HOME/.config/tmux/plugins/tpm/tpm'

# tab title: only window name, no index/flags
set -g window-status-format         '#[fg=#262626,bg=#3a3a3a] #[fg=#a7c080,bg=#3a3a3a] #W #[fg=#3a3a3a,bg=#262626] '
set -g window-status-current-format '#[fg=#262626,bg=#a7c080] #[fg=#262626,bg=#a7c080,bold] #W #[fg=#a7c080,bg=#262626,none] '

# fallback: source dark-notify theme if available
if-shell "test -e ~/.local/state/tmux/tmux-dark-notify-theme.conf" \
    "source-file ~/.local/state/tmux/tmux-dark-notify-theme.conf"

